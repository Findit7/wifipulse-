import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary branding
  static const Color primary = Color(0xFF4F46E5); // Vibrant Indigo
  static const Color primaryLight = Color(0xFF818CF8);
  static const Color primaryDark = Color(0xFF3730A3);

  // Backgrounds (Dark mode optimized)
  static const Color background = Color(0xFF0F172A); // Deep Slate
  static const Color surface = Color(0xFF1E293B);
  static const Color surfaceHighlight = Color(0xFF334155);

  // States
  static const Color success = Color(0xFF10B981); // Green online pulse
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444); // Red block action
  static const Color info = Color(0xFF3B82F6);

  // Text
  static const Color textPrimary = Color(0xFFF8FAFC);
  static const Color textSecondary = Color(0xFF94A3B8);
  static const Color textDisabled = Color(0xFF475569);
}
